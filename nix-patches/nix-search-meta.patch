diff --git a/src/nix/search.cc b/src/nix/search.cc
index 0d8fdd5c2..5fa3f935b 100644
--- a/src/nix/search.cc
+++ b/src/nix/search.cc
@@ -9,6 +9,7 @@
 #include "shared.hh"
 #include "eval-cache.hh"
 #include "attr-path.hh"
+#include "value-to-json.hh"
 
 #include <regex>
 #include <fstream>
@@ -131,6 +132,11 @@ struct CmdSearch : InstallableCommand, MixJSON
                             jsonElem.attr("pname", name.name);
                             jsonElem.attr("version", name.version);
                             jsonElem.attr("description", description);
+
+                            auto jsonMeta = jsonElem.placeholder("meta");
+
+                            PathSet context;
+                            printValueAsJSON(*state, true, aMeta->forceValue(), noPos, jsonMeta, context);
                         } else {
                             auto name2 = hilite(name.name, nameMatch, "\e[0;2m");
                             if (results > 1) logger->cout("");
